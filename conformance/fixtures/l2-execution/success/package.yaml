mtp_version: "0.2"
package:
  id: "20000000-0000-4000-8000-000000000001"
  name: "Conformance Success Fixture"
  version: "1.0.0"
  created: "2026-03-09T00:00:00Z"
  author: "conformance-suite"
intent:
  goal: "Verify successful deterministic execution for conformance."
input:
  description: "Synthetic CSV input for conformance execution."
methodology:
  approach: "Two-step deterministic execution without deviations or failures."
  steps:
    - step: 1
      name: "Step One"
      action: "Run a deterministic conformance analysis for fixture success."
      provenance:
        source_type: "manual"
        source_ref: "fixture-success-step-1"
        confidence: "high"
      execution_semantics:
        on_success: "proceed"
        on_failure: "halt"
        on_deviation: "flag_and_proceed"
      validation: "Step must produce a structured success result."
    - step: 2
      name: "Step Two"
      action: "Summarize the deterministic conformance analysis for fixture success."
      depends_on: [1]
      provenance:
        source_type: "manual"
        source_ref: "fixture-success-step-2"
        confidence: "high"
      execution_semantics:
        on_success: "proceed"
        on_failure: "halt"
        on_deviation: "flag_and_proceed"
      validation: "Summary must produce a structured success result."
output:
  description: "Synthetic conformance output."
  quality_checks:
    - check: "All steps produce structured output"
      failure_action: "Fail conformance."
      is_blocking: true
policy:
  data_classification: "internal"
  redaction:
    status: "passed"
  pii_scan:
    status: "passed"
  secrets_scan:
    status: "passed"
  client_identifier_scan:
    status: "passed"
  regulated_content:
    status: "passed"
